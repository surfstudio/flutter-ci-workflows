name: Show Me json
on: [push]

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest
    steps:
    - name: Set up secret file
      env:
        CON_STRING: ${{ secrets.PUB_CREDENTIAL_JSON }}
      run: |
        echo $CON_STRING >> con_json.txt
        echo $CON_STRING
